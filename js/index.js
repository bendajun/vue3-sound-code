import {
  reactive,
  shallowReactive,
  readonly,
  shallowReadonly,
} from './reactive'

import {
  effect
} from './effect'

import{
  ref,
  shallowRef
} from './ref'

import {
  toRef,
  toRefs
} from './toRef'

export default {
  reactive,
  shallowReactive,
  readonly,
  shallowReadonly,
  effect,
  ref,
  shallowRef,
  toRef,
  toRefs,
}