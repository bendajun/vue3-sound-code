import {
  reactive,
  shallowReactive,
  readonly,
  shallowReadonly,
} from './reactive'

import {
  effect
} from './effect'

export default {
  reactive,
  shallowReactive,
  readonly,
  shallowReadonly,
  effect,
}